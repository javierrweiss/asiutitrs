/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"BE970C7C-3A4A-4923-AAEB-256C50F00268"}
 */
function onAction_especial_N(event) {
	globals.vEspecial=foundset.esp_codi
	globals.vDescriEspecial=foundset.esp_descrip
	var name=application.getActiveWindow()
	name.getName()
	var $win=application.getWindow(name.getName())
	if($win!=null){
		$win.hide()
		$win.destroy()
	}
}
