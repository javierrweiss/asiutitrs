/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"F753CE6C-0212-4BD9-A30B-9EFA8EAF5699"}
 */
function onAction_record_selection(event) {
	globals.per_1=foundset.per_1
	globals.per_1_str=foundset.legajo1
	var name=application.getActiveWindow()
	name.getName()
	var $win=application.getWindow(name.getName())
	if($win!=null){
		$win.hide()
		$win.destroy()
	}
	//application.closeAllWindows()
}
