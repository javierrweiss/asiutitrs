/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"820ED049-F9F9-4492-A813-A56B2B5158F6"}
 */
function onAction_consulta(event) {
	// TODO Auto-generated method stub
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"E6EBB72E-088C-42CA-88AC-116ED444BF81"}
 */
function onAction_btn_consulta(event) {
	elements.btn_nuevo.enabled=false;
    elements.lbl_nuevo.enabled=false;
    elements.btn_modifica.enabled=true;
    elements.lbl_modifica.enabled=true;
    elements.btn_graba.enabled=false;
    elements.lbl_graba.enabled=false;
    application.updateUI()
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"641085C5-A5B1-46B0-8CD2-E312DF59F212"}
 */
function onAction_btn_agrega(event) {
	// TODO Auto-generated method stub
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"F4E5CDC3-4403-49B8-848D-92515AD68EF0"}
 */
function onAction_btn_modifica(event) {
	elements.btn_nuevo.enabled=false;
    elements.lbl_nuevo.enabled=false;
    elements.btn_modifica.enabled=false;
    elements.lbl_modifica.enabled=false;
    elements.btn_graba.enabled=true;
    elements.lbl_graba.enabled=true;
    elements.btn_consulta.enabled=false;
    elements.lbl_consulta.enabled=false;
    globals.tit_estado_tratamiento='Modifica'
    application.updateUI()
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"9755FA4B-6AB3-4EAB-A06C-D14FDA57B262"}
 */
function onAction_btn_graba(event) {
	elements.btn_nuevo.enabled=false;
    elements.lbl_nuevo.enabled=false;
    elements.btn_modifica.enabled=false;
    elements.lbl_modifica.enabled=false;
    elements.btn_graba.enabled=false;
    elements.lbl_graba.enabled=false;
    elements.btn_consulta.enabled=false;
    elements.lbl_consulta.enabled=false;
    application.updateUI()
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"751C6057-18EF-4EA6-B918-647E6580CA44"}
 */
function onAction_btn_imprime(event) {
	// TODO Auto-generated method stub
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"3305BDF5-CDDC-4227-9998-2CE7C7AE2296"}
 */
function onAction_btn_refresh(event) {
	elements.btn_nuevo.enabled=true;
    elements.lbl_nuevo.enabled=true;
    elements.btn_modifica.enabled=false;
    elements.lbl_modifica.enabled=false;
    elements.btn_graba.enabled=false;
    elements.lbl_graba.enabled=false;
    application.updateUI()
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"E1C83A3E-1E11-44F1-8FDF-5CA30193E8AD"}
 */
function onAction_btn_cancela(event) {
	application.exit()
	
}

/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"4D9BE858-B3EB-406C-8314-7E3AD9E0E3F1"}
 */
function onAction_btn_nuevo(event) {
	elements.btn_nuevo.enabled=false;
    elements.lbl_nuevo.enabled=false;
    elements.btn_modifica.enabled=false;
    elements.lbl_modifica.enabled=false;
    elements.btn_graba.enabled=false;
    elements.lbl_graba.enabled=false;
    elements.btn_consulta.enabled=false;
    elements.lbl_consulta.enabled=false;
    application.updateUI()
}
