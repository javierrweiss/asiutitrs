/**
 * Perform the element default action.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"A248C3DF-2875-45E6-BF67-8B4A08B8C626"}
 */
function onAction_Scroll_Interven(event) {
	globals.vtipnome = 1
	globals.vcodnome = forms.frm_scroll_imacodi.icodcodinom
	//var win = application.getWindow()
	//win.hide()
	//application.closeAllWindows();
	var name=application.getActiveWindow()
	name.getName()
	var $win=application.getWindow(name.getName())
	if($win!=null){
		$win.hide()
		$win.destroy()
	}
	//forms.frm_menu.elements.vcodnome.requestFocus()
    //controller.show(null);
}
