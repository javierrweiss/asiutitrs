/**
 * Handle hide window.
 *
 * @param {JSEvent} event the event that triggered the action
 *
 * @returns {Boolean}
 *
 * @properties={typeid:24,uuid:"465E03D7-C41B-4443-A5E3-9D71317D67DE"}
 */
function onHide(event) {
	return false
}

/**
 * Callback method for when form is shown.
 *
 * @param {Boolean} firstShow form is shown first time after load
 * @param {JSEvent} event the event that triggered the action
 *
 * @properties={typeid:24,uuid:"BAAAED3A-C351-4D37-812C-92C490E9B2DF"}
 */
function onShow(firstShow, event) {
	// TODO anulado por ahora 
	/*
	if (scopes.globals.AtCons_histcli_disponible == 0)
		{scopes.globals.Mensaje_HistoriaClinica()
		scopes.globals.AtCons_histcli_disponible = 1}
	*/
}
